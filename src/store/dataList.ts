import { createSlice } from '@reduxjs/toolkit'
const dataList = createSlice( {
    name: 'dataList',
    initialState: {},
    reducers:{}
} )
export default dataList.reducer
export const {}=dataList.actions